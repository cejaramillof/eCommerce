.card.large-padding.big-padding
  %h1 Lista de Productos
  - @products.each do |product|
    .col-md-4.text-center.top-space
      .other-grey.card.big-padding
        .col-xs-6
          %p
            Nombre:
            = product.name
        .col-xs-6            
          %p
            Precio:
            = product.pricing
        .col-xs-12
          .card-action
            = link_to 'Ver', product_path(id: product.id, client:true), class: "btn btn-info"
            = render "in_shopping_carts/form",product: product